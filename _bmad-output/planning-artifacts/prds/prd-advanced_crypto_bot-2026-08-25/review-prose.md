# Editorial Review — Prose

Reader type: `humans`
Scope: `prd.md` dan `addendum.md`
Approach: minimal intervention; technical English terms, normative capitalization, stable IDs, and deliberate bilingual voice are preserved.

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| PRD ini mendefinisikan pengganti penuh AutoTrade untuk Officer, workflow produk/arsitektur, dan implementer. | PRD ini mendefinisikan pengganti penuh AutoTrade bagi Officer serta untuk workflow produk/arsitektur dan para implementer. | Clarifies that the three audiences/uses are distinct; removes ambiguity created by the mixed list. |
| Semua keputusan, order, fill, posisi, cash, risiko, dan outcome berasal dari fakta canonical yang dapat direkonstruksi. | Semua keputusan, order, fill, Position, cash, risiko, dan outcome berasal dari fakta canonical yang dapat direkonstruksi. | Aligns `Position` with the document's defined, capitalized domain term. The same capitalization should be applied to lowercase uses that refer specifically to the defined entity. |
| Strategy legacy boleh menjadi Champion hanya setelah canonical conformance dan seluruh evidence gate lulus. | Strategy legacy boleh menjadi Champion hanya setelah memenuhi canonical conformance dan lulus seluruh evidence gate. | Restores parallel verb structure and makes both conditions explicit. |
| Entry dibatalkan/dilatch dan risk-reduction EXIT menuju zero exposure | Entry dibatalkan, entry latch ditutup, dan risk-reduction EXIT dimulai menuju zero exposure | Replaces the hybrid verb `dilatch` with the named control and clarifies that EXIT is initiated rather than guaranteed immediately. |
| Real-money Order submission; deferred ke live-readiness PRD terpisah. | Submission Order dengan uang asli; ditunda hingga PRD live-readiness terpisah. | Converts the fragment into clearer Indonesian while preserving the domain term `Order`. |
| Dynamic allocation lintas banyak strategy. | Dynamic allocation di antara banyak strategy. | Clarifies the relationship expressed by `lintas`. |
| Platform-ready: canonical replay/accounting/invariants, migration, recovery, kill, and fault-injection acceptance lulus; tidak memerlukan strategy profit. | **Platform-ready:** acceptance untuk canonical replay, accounting, invariants, migration, recovery, kill, dan fault injection lulus; tahap ini tidak memerlukan strategy yang profitable. | Makes `acceptance` govern the complete list and clarifies the final clause. Apply the same language convention consistently to the other gate descriptions. |
| Reuse only behind adapters: Indodax clients, market collectors, Telegram/dashboard surfaces, and legacy strategy calculations; reuse tidak memberi write authority. | **Reuse only behind adapters:** Indodax clients, market collectors, Telegram/dashboard surfaces, dan legacy strategy calculations. Komponen yang digunakan kembali tidak memperoleh write authority. | Removes a vague pronoun-like repetition (`reuse`) and separates two distinct rules. |
| Memutuskan promotion/demotion berdasarkan bukti out-of-sample yang tersegel. | Memutuskan promotion atau demotion berdasarkan bukti out-of-sample yang tersegel. | Replaces slash construction with an explicit alternative for easier reading. |
| Candidate Snapshot → Canonical Decision → Intent → Order → Fill → Position → outcome dalam satu correlation trail. | Candidate Snapshot → Canonical Decision → Intent → Order → Fill → Position → outcome sebagai satu correlation trail. | Fixes the missing predicate relationship after `melihat`/`dapat melihat` uses elsewhere and clarifies that the sequence constitutes the trail. Apply at FR-28 as: `Officer dapat melihat ... sebagai satu correlation trail.` |
| Snapshot/replay manifest juga memuat ordered input event IDs, scheduler trigger, clock values yang dibaca Policy, RNG algorithm/state, numeric/runtime/environment versions, instrument metadata, dan external responses yang memengaruhi hasil. | Snapshot/replay manifest juga memuat ordered input event IDs, scheduler trigger, nilai clock yang dibaca Policy, algorithm dan state RNG, versi numeric/runtime/environment, instrument metadata, serta external responses yang memengaruhi hasil. | Makes compound list items easier to parse and removes ambiguous slash groupings. |
| Acceptance membandingkan serialized decision/event bytes setelah mengabaikan hanya explicitly non-semantic fields; perbedaan semantic field apa pun adalah failure. | Acceptance membandingkan serialized decision/event bytes dengan mengabaikan hanya field yang secara eksplisit dinyatakan non-semantic. Perbedaan pada semantic field apa pun merupakan failure. | Reduces code-switching inside modifiers and splits an overloaded sentence into the comparison rule and failure rule. |
| Policy hanya `ENTER` atau mempertahankan exposure ketika net edge setelah biaya melampaui uncertainty boundary yang dipraregistrasi; selain itu `ABSTAIN`/`HOLD`. | Policy hanya melakukan `ENTER` atau mempertahankan exposure ketika net edge setelah biaya melampaui uncertainty boundary yang dipraregistrasi; selain itu, Policy memilih `ABSTAIN` atau `HOLD` sesuai state. | Supplies missing verbs and clarifies that `ABSTAIN` and `HOLD` are state-dependent alternatives, not interchangeable labels. |
| Precedence tertinggi ke rendah: operator kill/emergency zeroing → hard drawdown risk reduction → hard invalidation/stop → reconciliation-forced action → partial profit/trailing/time stop → alpha EXIT → HOLD. | Urutan precedence dari tertinggi ke terendah adalah: operator kill atau emergency zeroing → hard-drawdown risk reduction → hard invalidation atau stop → reconciliation-forced action → partial profit, trailing, atau time stop → alpha EXIT → HOLD. | Adds the missing connective phrase and expands slash-separated alternatives so the sequence is unambiguous. |
| Setiap Order harus mencapai `FILLED`, `CANCELLED`, `REJECTED`, atau `EXPIRED`. `UNKNOWN` adalah recovery state dengan bounded query/retry deadline `[ASSUMPTION]` 60 detik; selama UNKNOWN, entry untuk account/pair dibekukan dan resubmit dilarang sampai reconciliation membuktikan terminal state. | Setiap Order harus mencapai `FILLED`, `CANCELLED`, `REJECTED`, atau `EXPIRED`. `UNKNOWN` adalah recovery state dengan batas waktu query/retry `[ASSUMPTION]` 60 detik. Selama state ini, entry untuk account dan pair terkait dibekukan, dan resubmission dilarang sampai reconciliation membuktikan terminal state. | Breaks up a dense operational rule and clarifies the scope of the freeze. |
| Sistem membandingkan journal internal dengan recovery evidence; mismatch membekukan entry dan menghasilkan correction workflow teraudit, bukan overwrite diam-diam. | Sistem membandingkan journal internal dengan recovery evidence. Mismatch membekukan entry dan memulai correction workflow yang teraudit; sistem tidak menimpa data secara diam-diam. | Makes the prohibition explicit and avoids a noun (`overwrite`) functioning ambiguously as a verb/action. |
| Snapshot minimum memuat ordered bid/ask depth sampai melampaui requested size, sequence/offset, event/receive time, instrument metadata version, dan source. | Snapshot minimum memuat ordered bid/ask depth yang cukup untuk melampaui requested size, sequence dan offset, event time dan receive time, instrument metadata version, serta source. | Clarifies the depth requirement and expands slash pairs into distinct required fields. |
| Equity peak adalah high-water mark yang disesuaikan external cash flow dan tidak dapat diturunkan/reset kecuali memulai Experiment/portfolio baru dengan approval teraudit. | Equity peak adalah high-water mark yang disesuaikan terhadap external cash flow. Nilai ini tidak dapat diturunkan atau direset, kecuali ketika memulai Experiment atau portfolio baru dengan approval yang teraudit. | Resolves an overloaded sentence and clarifies what cannot be changed and under what exception. |
| Quantity di bawah venue minimum menjadi quarantined dust dengan incident/valuation, bukan dianggap CLOSED. | Quantity di bawah minimum venue diklasifikasikan sebagai quarantined dust, disertai incident dan valuation; quantity tersebut tidak dianggap `CLOSED`. | Adds the missing action and clarifies that both records are required. |
| Dynamic eligibility formula dan effective-dated membership juga dibekukan point-in-time. | Formula dynamic eligibility dan effective-dated membership juga dibekukan pada satu point in time. | Fixes modifier order and supplies the missing preposition. |
| MVP Horizon IDs adalah `[ASSUMPTION]` `15M`, `1H`, `4H`, dan `1D`; label maturities adalah 1h, 4h, 24h, dan 72h yang applicable. | MVP Horizon IDs adalah `[ASSUMPTION]` `15M`, `1H`, `4H`, dan `1D`. Label maturity yang berlaku adalah 1h, 4h, 24h, dan 72h. | Removes an awkward trailing modifier and separates two enumerations. |
| Report membekukan sampling unit (satu lifecycle Position; partial exits bukan trade baru), exposure-overlap clusters/effective sample size, regime detector/version, required regime observations, return series, benchmark, bootstrap/CV method, multiple-testing family, estimators, dan conjunctive pass logic sebelum window dibuka. | Sebelum window dibuka, Report membekukan: sampling unit (satu lifecycle Position; partial exit bukan trade baru), exposure-overlap clusters dan effective sample size, regime detector dan version, required regime observations, return series, benchmark, bootstrap/CV method, multiple-testing family, estimators, serta conjunctive pass logic. | Moves the timing condition forward and disambiguates paired list items. |
| Profit concentration memakai absolute positive contribution dan exposure/risk concentration dengan explicit treatment saat aggregate profit ≤0. | Profit concentration menggunakan absolute positive contribution. Exposure/risk concentration menggunakan treatment yang ditetapkan secara eksplisit ketika aggregate profit ≤0. | Separates two metrics whose shared predicate is unclear. Consider confirming whether `exposure/risk concentration` is one combined metric or two metrics. |
| `NFR-5 Performance:` `[ASSUMPTION]` Canonical Decision selesai sebelum snapshot melewati freshness budget; angka ditetapkan architecture. | **NFR-5 Performance:** `[ASSUMPTION]` Canonical Decision selesai sebelum snapshot melewati freshness budget; batas numeriknya ditetapkan dalam architecture. | Replaces the vague noun `angka` with the exact concept being set. |
| Tidak ada satu pair menyumbang >35% absolute positive contribution atau >35% aggregate risk/exposure contribution; aggregate net profit ≤0 selalu gagal. | Tidak ada satu pair yang boleh menyumbang lebih dari 35% absolute positive contribution atau lebih dari 35% aggregate risk/exposure contribution. Aggregate net profit ≤0 selalu gagal. | Adds the missing relative marker and separates the independent failure condition. |
| Seluruh baseline assumption di bawah harus divalidasi oleh architecture/telemetry sebelum statusnya menjadi normatif. | Seluruh baseline assumption di bawah harus divalidasi melalui architecture atau telemetry sebelum ditetapkan sebagai requirement normatif. | Clarifies that architecture/telemetry are validation mechanisms and names what becomes normative. |
| Detail teknis, opsi, dan sumber primer yang memengaruhi PRD tetapi bukan requirement tingkat atas. | Addendum ini memuat detail teknis, opsi, dan sumber primer yang memengaruhi PRD, tetapi bukan merupakan requirement tingkat atas. | Converts the opening fragment into a complete sentence and makes the exclusion explicit. |
| Kandidat spine: calibrated predictive distribution → net-cost utility → abstention/no-trade band → persisted state transition. | Candidate policy spine adalah: calibrated predictive distribution → net-cost utility → abstention/no-trade band → persisted state transition. | Replaces the potentially confusing phrase `Kandidat spine` with a named technical concept. |
| Numeric gates adalah pre-registration proposals. DSR/PBO/calibration tidak membuktikan future profit. | Numeric gates merupakan pre-registration proposals. DSR, PBO, dan calibration tidak membuktikan profit pada masa depan. | Clarifies that the slash-separated items are three distinct methods/measurements and translates the final temporal phrase for readability. |
